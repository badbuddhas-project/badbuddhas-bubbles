# 05. Реестр медиа-ассетов

Сейчас runtime-медиа (аудио + обложки практик) лежит в **Supabase Storage** (публичные бакеты `audio/`, `images/`) и отдаётся прямыми URL из полей `practices.audio_url` / `preview_image_url` / `instructor_avatar_url`. Никаких подписанных URL — файлы публичны, если известна ссылка.

**В Chatium:** загрузить все файлы в **медиасервис Chatium**, получить новые URL, прописать их в Heap-таблицу `practices`. Аудио проверить на seek и фоновое воспроизведение (см. `04-design.md`).

## Аудио-мастера (`badbuddhas-content/`)

Исходные mp3 практик (это источник, не то, что сейчас отдаётся в проде):

| Файл | Ориентировочная практика |
|---|---|
| `Here_and_now.mp3` | «Здесь и сейчас» |
| `Realese_stress.mp3` | «Снять стресс» |
| `Transform.mp3` | «Трансформация» |
| `Breathe_out.mp3` | «Выдохнуть» |

> Точное сопоставление «файл → запись в `practices`» (id, категория, инструктор, `is_premium`, длительность) нужно взять из **актуальной таблицы `practices`** в Supabase на момент миграции — каталог наполняется вручную и мог измениться. Экспортируйте `practices` (см. `07-data-migration.md`) и сверьте.

## Картинки инструкторов / категорий (`badbuddhas-content/`)

| Файл | Назначение |
|---|---|
| `dasha.png` | Аватар инструктора (Даша) |
| `balance.png` | Иллюстрация категории balance |
| `energize.png` | Иллюстрация категории energize |
| `fallasleep.png` | Иллюстрация практики «уснуть» |
| `photo_2026-01-27 10.38.47 1.png` | Фото (уточнить назначение) |

## Статические UI-ассеты (`public/images/`)

| Файл | Назначение |
|---|---|
| `logo.svg`, `logo-white.svg` | Логотип |
| `logo-square.png`, `logo-white-square.png`, `logo-ribs.png` | Логотип (варианты) |
| `icon-black.png` | Иконка |
| `black_blob_5.png` | Статичный блоб (запасной вариант анимации) |
| `onboarding-blob.png` | Блоб в онбординге |
| `onboarding-community.png`, `onboarding-cycle.png` | Иллюстрации онбординга |
| `plohie_buddy_rus.png` | Иллюстрация (онбординг/маркетинг) |

## Актуальный каталог уже выгружен

Полный дамп таблицы `practices` (36 записей с реальными URL аудио/обложек, категориями `slow`/`ground`/`rise`, флагами `is_premium`/`is_visible`, описаниями) и сами аудиофайлы уже выгружены в папку **`supabase-export/`** (в `.gitignore`, передаётся отдельно): `practices.json`, аудио в `dasha_chen_audio/` и `other_practices_audio/<инструктор>/`, таблицы сопоставления `full_track_map.csv` / `full_track_map.md`. Это авторитетный источник — актуальнее, чем мастера в `badbuddhas-content/`.

## Что сделать при переезде

1. Взять актуальный каталог из `supabase-export/practices.json` (уже выгружен).
2. Взять аудио из `supabase-export/` (уже скачано); сверять «файл → название» по `full_track_map.csv` — имена файлов техничные и не совпадают с названиями в приложении.
3. Загрузить всё в медиасервис Chatium.
4. Прописать новые URL в Heap `practices`.
5. Проверить каждую практику: играет, seek работает, обложка на месте.
