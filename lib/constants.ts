/**
 * Shared application constants.
 * Single source of truth for string keys and configuration values used across multiple files.
 */

/** localStorage key that marks onboarding as completed. */
export const ONBOARDING_KEY = 'badbuddhas_onboarding_completed'
